{
  "name": "jersey-stock-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run electron:dev --workspace=desktop"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.105.4",
    "electron-serve": "1.1.0",
    "prisma": "^7.8.0"
  }
}
