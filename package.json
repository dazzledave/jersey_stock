{
  "name": "jersey-stock-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run electron:dev --workspace=awards-centre-pos",
    "dist": "npm run dist --workspace=awards-centre-pos"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.105.4",
    "electron-serve": "1.1.0",
    "prisma": "^7.8.0"
  }
}
